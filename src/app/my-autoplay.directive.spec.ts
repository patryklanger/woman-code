import { MyAutoplayDirective } from './my-autoplay.directive';

describe('MyAutoplayDirective', () => {
  it('should create an instance', () => {
    const directive = new MyAutoplayDirective();
    expect(directive).toBeTruthy();
  });
});
