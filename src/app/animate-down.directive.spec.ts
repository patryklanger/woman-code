import { AnimateDownDirective } from './animate-down.directive';

describe('AnimateDownDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateDownDirective();
    expect(directive).toBeTruthy();
  });
});
