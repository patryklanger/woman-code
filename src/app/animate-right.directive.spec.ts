import { AnimateRightDirective } from './animate-right.directive';

describe('AnimateRightDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateRightDirective();
    expect(directive).toBeTruthy();
  });
});
